package functions

object CountSubsets {

  // TASK:
  //   countFixedSizedSubsets(n,m)
  //      Use factorial to compute the number of n-sized subsets of an m-element set.
  //      Use your function to write some tests for the n-sized subset
  //      function which was developed in class.
  def countFixedSizedSubsets(superSetSize: Int, subSetSize: Int): Int = {
    ???
  }

  // We implement this function in the lecture.
  def fixedSizedSubsets[T](n:Int, objects:Set[T]):Set[Set[T]] = {
    ???
  }

  def main(argv: Array[String]): Unit = {

  }
}
